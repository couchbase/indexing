syntax = "proto2";

package protoProjector;
option go_package = "github.com/couchbase/indexing/secondary/protobuf/projector;protoProjector";

message KeyPartition {
    required uint64 numPartition   = 1;
    repeated uint64 partitions     = 2;
    repeated string endpoints      = 3;
}
